extends layout
include mixins/video

block content
    form(method="GET")
        input(name="keyword", placeholder="Search by Title", type="text", value=keyword)
        input(type="submit", value="Search")

    each video in videos
        +video(video)
    else
        p Nothing Found